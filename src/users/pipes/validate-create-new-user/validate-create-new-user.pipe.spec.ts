import { ValidateCreateNewUserPipe } from './validate-create-new-user.pipe';

describe('ValidateCreateNewUserPipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateNewUserPipe()).toBeDefined();
  });
});
